// Re-export link-plan functions for unlink domain cohesion.
// Unlink-specific pure functions can be added here later if needed.
export { buildLinkEntries, getLinkableWorkspaces, type LinkEntry, validateLinkMap, validateLinkPath } from "./link-plan.ts";
